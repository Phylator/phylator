- replace :layout, 'app'
- replace :body_class, 'myg-layout--toolbar myg-layout--toolbar-bottom'

= mozaic do

    = component 'myg/toolbar', class: 'myg-toolbar--flexible', data: { turbolinks_animate_persist: 'app' } do
        .mdc-toolbar__row
            %section.mdc-toolbar__section.mdc-toolbar__section--align-center
                -# ...

    .myg-content
        = mozaic_area :app do
            = yield

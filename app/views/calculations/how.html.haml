%h1.sm Choose a set of measurements
%p Here are suggestions for measurements you could enter.
.flex.center
    .myg-list
        - @equations.each do |equation|
            .myg-list--item= link_to equation.quantities.map { |q| q.name }.join(', '), enter_url(quantity: params[:quantity], unit: params[:unit], equation: equation.id)
    %p.sm or
    = link_to 'Enter custom measurements', enter_url(quantity: params[:quantity], unit: params[:unit])
